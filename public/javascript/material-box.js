document.addEventListener('DOMContentLoaded', function() {
    M.AutoInit();
  
    var options = {

    };
    var elems = document.querySelector('.materialboxed.no-autoinit');
    var instances = M.Materialbox.init(elems, options);
  })

